# Local smoke environment: the same catalog, pointed at a temporary directory.
#
# `VEC2VEC_TEST_ROOT` is set by the test suite (and can be set by hand to run
# the pipelines against a small local fixture: `kedro run --env test`).
raw: ${oc.env:VEC2VEC_TEST_ROOT}/raw
lake: ${oc.env:VEC2VEC_TEST_ROOT}/lake
